# Supabase keep-alive notes

Supabase Free projects can pause after inactivity. A scheduled GitHub Actions workflow can ping the project twice per week using the Supabase anon key stored as a repository secret.
